// When page loads
window.addEventListener("load", function(){
  // When the user submits the form
  document.addEventListener("submit", function(event){
    // Hide the form
    // Display the list of items in alphabetical order, all uppercased
  });

});
